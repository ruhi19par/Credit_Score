# CredBridge AI Frontend

Minimal React/Vite frontend for the Spring Boot Basic Mode MVP.

## Run

```bash
npm install
npm run dev
```

The dev server runs on `http://localhost:3000` and expects the backend at `http://localhost:8080`.
Set `VITE_API_BASE_URL` if the backend uses a different URL.
